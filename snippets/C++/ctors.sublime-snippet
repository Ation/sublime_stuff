<snippet>
    <content><![CDATA[
public:
   ${1}();
   ~${1}() noexcept;

   ${1}(const ${1}&) = delete;
   ${1}& operator = (const ${1}&) = delete;

   ${1}(${1}&&) = delete;
   ${1}& operator = (${1}&&) = delete;
]]></content>
    <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
    <tabTrigger>ctors</tabTrigger>
    <!-- Optional: Set a scope to limit where the snippet will trigger -->
    <scope>source.c++</scope>
</snippet>
